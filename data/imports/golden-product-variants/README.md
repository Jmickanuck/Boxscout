# Golden Product variant pilot

Reviewed 2026-09-14. input.json stores minimal factual row extracts, source inventory/locators, complete-family definitions, independent identity corroboration and a review record. No copied webpages, scans or workbook binary is committed. The GTS workbook hash is recorded in sources; the original temporary Plan 003 source was reused. All 59 added identities agree with Checklist Insider. Source-specific limitations are retained under review.

Replay from the repository root:

```text
npm run variants:generate
npm run variants:check
npm test
```

The generated typed fixture records the input hash and fails the check if stale. prebuild checks base, variants and images. scripts/checklists/variants.ts accepts a release manifest and existing canonical base records; source names/columns are adapter input, not UI code. Future releases reuse validation, serial representation, source references and eligibility claims. A synthetic second-release test exercises that boundary.

To revise evidence: obtain authorized source material temporarily; extract exact named sets and preserve row numbers; compare independent identity lists; review numbering and exceptions; update factual input and review rationale together; run checks and review the diff. Do not merely change the expected count to bless a partial family. Do not infer numbering from an empty workbook cell: the pilot's unnumbered classification was corroborated by the published checklist. Exact autograph Silver per-player totals rely on manufacturer rows; the secondary list confirms /199 or /75 at family level. Unknown evidence must not become VERIFIED.

Defaults: 500 Base + 25 Base Variations + 25 Aces + nine Team USA signatures. Parallel families: 500 Red Disco /99, 500 Black /1, 25 Aces Silver, 25 Aces Orange /25, 25 Aces Green /5, nine Team USA Silver. Total 559 entries / 1,643 variants. Family coverage is deliberate and complete; no finite physical copies are created. The original base fixture and its reviewed name corrections are not regenerated by this importer.

Only the NPP Red Disco inclusion rule is currently supported (500 exact edges). Manufacturer-family verification is not an exact Mastermind UPC match. Other configuration eligibility is unknown. Existing average/guarantee conflicts remain in Plan 002 data. No new images or infrastructure.

Source refresh adapter: scripts/checklists/extract-prizm-variants.py accepts temporary XLS/HTML and emits unapproved factual input to a separate file. It does not download or retain pages. Recheck hashes/dates, compare changes and approve the review explicitly before generation.
